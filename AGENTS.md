# Agent 接续说明

开始修改代码前必须：

1. 检查最新用户指令、`git status`、当前分支和远程状态；不得回滚或混入未知改动。
2. 获取维护者当前提供的进度、约束和任务优先级；不得仅凭 README、Git 历史或 Issue
   推断未提交的工作状态。
3. 只领取依赖已完成且优先级最高的任务；任务状态不明确时先请求恢复。
4. 完成任务时提供对应测试或人工证据，记录实现提交 SHA，并同步维护者的交接信息。

如果本地交接信息缺失、内容冲突或明显过期，停止猜测进度并向用户请求恢复。

## Git 与发布边界

- 测试和审查通过后，Agent 可以将任务提交推送到 `main`。
- 不得强推、改写历史、绕过失败测试，或把用户媒体、模型、缓存、构建产物和秘密加入 Git。
- 不得自动创建正式 `v*` tag 或 GitHub Release。每次正式发布都需要用户明确批准版本和提交。
- nightly/debug 工作流只上传测试或诊断 artifacts，不创建正式 Release。

## 工程要求

- 优先使用现有模式、锁定依赖和结构化 API，不进行无关重构。
- 用户可见文档使用中文；代码标识、Schema 字段和命令使用 ASCII。
- 所有正式协议必须版本化，并让 Rust、Python 与播放器交接包使用同一组 fixtures。
- 未实现、未验证或跳过的内容不得写成已完成。
